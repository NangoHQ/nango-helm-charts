{{- if .Values.metering.enabled }}
{{- if .Values.metering.service.create }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "metering.names.service" . }}
  namespace: {{ include "metering.names.namespace" . | quote }}
  labels: {{- include "common.labels.standard" ( dict "customLabels" .Values.commonLabels "context" $ ) | nindent 4 }}
    app.kubernetes.io/component: {{ include "metering.names.component" . }}
  {{- if or .Values.metering.service.annotations .Values.commonAnnotations }}
  {{- $annotations := include "common.tplvalues.merge" (dict "values" (list .Values.metering.service.annotations .Values.commonAnnotations) "context" .) }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" $annotations "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.metering.service.type }}
  {{- if and .Values.metering.service.clusterIP (eq .Values.metering.service.type "ClusterIP") }}
  clusterIP: {{ .Values.metering.service.clusterIP }}
  {{- end }}
  {{- if .Values.metering.service.sessionAffinity }}
  sessionAffinity: {{ .Values.metering.service.sessionAffinity }}
  {{- end }}
  {{- if .Values.metering.service.sessionAffinityConfig }}
  sessionAffinityConfig: {{- include "common.tplvalues.render" (dict "value" .Values.metering.service.sessionAffinityConfig "context" $) | nindent 4 }}
  {{- end }}
  {{- if or (eq .Values.metering.service.type "LoadBalancer") (eq .Values.metering.service.type "NodePort") }}
  externalTrafficPolicy: {{ .Values.metering.service.externalTrafficPolicy | quote }}
  {{- end }}
  {{- if and (eq .Values.metering.service.type "LoadBalancer") (not (empty .Values.metering.service.loadBalancerSourceRanges)) }}
  loadBalancerSourceRanges: {{ .Values.metering.service.loadBalancerSourceRanges }}
  {{- end }}
  {{- if and (eq .Values.metering.service.type "LoadBalancer") (not (empty .Values.metering.service.loadBalancerIP)) }}
  loadBalancerIP: {{ .Values.metering.service.loadBalancerIP }}
  {{- end }}
  ports:
    - name: http
      port: {{ .Values.metering.service.ports.http }}
      {{- if not (eq .Values.metering.service.ports.http .Values.metering.containerPorts.http) }}
      targetPort: {{ .Values.metering.containerPorts.http }}
      {{- end }}
      protocol: TCP
      {{- if and (or (eq .Values.metering.service.type "NodePort") (eq .Values.metering.service.type "LoadBalancer")) (not (empty .Values.metering.service.nodePorts.http)) }}
      nodePort: {{ .Values.metering.service.nodePorts.http }}
      {{- else if eq .Values.metering.service.type "ClusterIP" }}
      nodePort: null
      {{- end }}
    {{- if .Values.metering.service.extraPorts }}
    {{- include "common.tplvalues.render" (dict "value" .Values.metering.service.extraPorts "context" $) | nindent 4 }}
    {{- end }}
  {{- $podLabels := include "common.tplvalues.merge" (dict "values" (list .Values.metering.podLabels .Values.commonLabels) "context" .) | fromYaml }}
  selector: {{- include "common.labels.matchLabels" ( dict "customLabels" $podLabels "context" $ ) | nindent 4 }}
    app.kubernetes.io/component: {{ include "metering.names.component" . }}
{{- end }}
{{- end }}